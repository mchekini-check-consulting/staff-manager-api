apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sm-api-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: sm-api-service
                port:
                  number: 8080
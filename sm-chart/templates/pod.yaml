apiVersion: v1
kind: Pod
metadata:
  name: my-first-pod
  labels:
    app: my-first-pod
spec:
  containers:
    - name: my-first-pod
      image: nginx
      ports:
        - containerPort: 80
      imagePullPolicy: Always
  restartPolicy: Always

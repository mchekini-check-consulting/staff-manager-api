apiVersion: v1
kind: Pod
metadata:
  name: my-first-pod
  labels:
    app: my-first-pod
spec:
  containers:
    - name: my-first-pod
      image: mchekini/sm-api:1.0
      ports:
        - containerPort: 8080
      imagePullPolicy: Always
  restartPolicy: Always
